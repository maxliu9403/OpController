## OpController v0.1.12

本次更新优化了更新弹窗的版本刷新逻辑：如果用户取消或跳过了一个旧更新，应用会重新检查 GitHub 最新版本，不会一直停留在上一次获取到的旧版本信息上。

跳过只会针对当前版本生效。后续发布更高版本时，应用会重新提示新版本；取消只会在当前会话里避免重复弹出同一个版本。

侧边栏的更新入口现在会先刷新最新版本再打开弹窗，避免用户看到已经过期的更新说明。

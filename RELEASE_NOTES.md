## OpController v0.1.20

本次更新修复 BitBrowser 测试窗口已打开，编辑或插入流程节点时仍提示“测试指纹窗口尚未打开”的问题。

- 正确读取正在运行的窗口及连接地址，恢复从真实页面点选元素、定位预览和流程试跑。
- 支持识别在 BitBrowser 中手动打开的窗口，并在窗口重启或关闭后更新测试会话状态。
- 统一测试会话的就绪判断，连接信息不完整或状态读取失败时，不再显示“已就绪”。

更新后，打开测试窗口并进入目标页面，即可继续编辑节点和点选元素。

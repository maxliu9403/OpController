## OpController v0.1.10

- **立即更新新增进度条**：点击更新后会展示准备、下载、安装、重启阶段，下载时显示百分比和已下载大小，减少等待过程中的不确定感。
- **更新安装体验优化**：安装过程中会锁定关闭、跳过和重复点击操作，按钮切换为“正在更新”，避免客户误操作打断升级。
- **更新弹窗视觉增强**：进度区域采用独立状态卡片和渐变进度条，整体更接近商业化桌面 App 的升级体验。
- **更新内容展示修复**：发布流程会在生成 `latest.json` 前读取 `RELEASE_NOTES.md`，确保应用内弹窗显示完整更新内容，而不是只显示版本号。
- **发布流程稳固**：继续保持 tag 触发发布，构建 macOS/Windows updater assets，并校验 source version 与 release tag 一致。

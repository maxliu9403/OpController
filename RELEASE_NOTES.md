## OpController v0.1.17

本次更新修复 macOS 内置更新在部分安装位置下失败的 `Cross-device link (os error 18)` 问题。

macOS 更新安装现在会在当前 `.app` 所在磁盘卷内完成解包、备份和替换，避免从系统临时目录跨卷移动应用导致失败。

如果客户端已经停留在旧错误版本且内置更新仍然失败，请手动安装一次本版本；安装到 v0.1.17 后，后续更新会继续走修复后的链路。
